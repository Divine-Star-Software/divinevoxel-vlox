export type VoxelShapeShapeDirections = "+x" | "-x" | "+y" | "-y" | "+z" | "-z";

export const VoxelShapeShapeDirectionsArray: VoxelShapeShapeDirections[] = [
  "+x",
  "-x",
  "+y",
  "-y",
  "+z",
  "-z",
];
